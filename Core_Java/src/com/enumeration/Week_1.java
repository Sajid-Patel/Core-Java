package com.enumeration;

//Test_1
/*
 About enum=
  enum are internally treated as class format
  enum are by default final : so we cannot create child enum
  enum are by default public
  the default super class of enum is Enum
  enum->keyword used to declare enum
  Enum-> is default Super class
  enum contains private constructor : so not possible to create the object
  enums to declare user defined constants
  every enum constant is object
 */
enum Week_1 {
	MON, TUE, WED; // by default these are public static final
}
